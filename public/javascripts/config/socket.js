define({
    host: 'http://localhost',
    port: '8080',
    options: {
        reconnection: false,
        reconnectionAttempts: 10, // Infinity
        autoConnect: true,
        timeout: 20000
    }
});
